using UnityEngine;

namespace Audio
{
    public class TestStartAudio : MonoBehaviour
    {
   
        private void Awake()
        {
        
        }

        void Start()
        {
            //AudioManager.StopSound("MainMusic");
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
